# Mini webapp Flask para Render
- Dashboard → New → Web Service → Git Provider → selecciona este repo → Create Web Service.
- O usa “Existing Image” si publicas la imagen en Docker Hub/GHCR.
Requisito: el servidor escucha en $PORT y 0.0.0.0.
